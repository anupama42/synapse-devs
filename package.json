{
  "name": "synapsedevs-portfolio",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "api": "npm start --prefix backend",
    "web": "npm start --prefix frontend",
    "start": "concurrently -n api,web -c cyan,magenta \"npm run api\" \"npm run web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
